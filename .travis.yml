language: node_js
before_install:
  - gem install sass
  - npm install -g grunt-cli
  - npm install -g bower
install:
  - npm install
  - bower install
  - cp config.js.example config.js
script: grunt
